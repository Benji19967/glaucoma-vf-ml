model:
  num_classes: 3
  learning_rate: 0.001
data:
  batch_size: 32
  num_workers: 4
trainer:
  max_epochs: 70
  logger:
    class_path: lightning.pytorch.loggers.TensorBoardLogger
    init_args:
      save_dir: logs
      name: hvf_system
